{
  "name": "mcp-ui-dashboard-example",
  "version": "1.0.0",
  "description": "Example MCP server demonstrating UI rendering capabilities with interactive dashboards",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc && node dist/index.js",
    "client": "tsc && node dist/client-example.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["mcp", "ui", "dashboard", "example", "model-context-protocol"],
  "author": "MCP UI Extension Example",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "file:../typescript-sdk"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
